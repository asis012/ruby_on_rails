<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id), :remote => true,  :authenticity_token => true, 
             html: { method: :delete }) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-success" %>
<% end %>